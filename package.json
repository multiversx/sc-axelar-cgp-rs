{
  "private": true,
  "scripts": {
    "build": "npm run build:gateway && npm run build:auth && npm run build:gas-service && npm run build:its && npm run build:interchain-token-factory && npm run build:token-manager && npm run build:ping-pong && npm run build:governance",
    "build:cgp": "npm run build:gateway && npm run build:auth && npm run build:gas-service",
    "build:gateway": "cd gateway && mxpy contract build",
    "build:auth": "cd auth && mxpy contract build",
    "build:gas-service": "cd gas-service && mxpy contract build",
    "build:its": "cd interchain-token-service && mxpy contract build",
    "build:interchain-token-factory": "cd interchain-token-factory && mxpy contract build",
    "build:token-manager": "cd token-manager && mxpy contract build",
    "build:governance": "cd governance && mxpy contract build",
    "build:ping-pong": "cd ping-pong-interchain && mxpy contract build",
    "build:ci": "npm run build:ci:gateway && npm run build:ci:auth && npm run build:ci:gas-service && npm run build:ci:its && npm run build:ci:interchain-token-factory && npm run build:ci:token-manager && npm run build:ci:ping-pong && npm run build:ci:governance",
    "build:ci:gateway": "cd gateway && python3 -m multiversx_sdk_cli.cli contract build",
    "build:ci:auth": "cd auth && python3 -m multiversx_sdk_cli.cli contract build",
    "build:ci:gas-service": "cd gas-service && python3 -m multiversx_sdk_cli.cli contract build",
    "build:ci:governance": "cd governance && python3 -m multiversx_sdk_cli.cli contract build",
    "build:ci:its": "cd interchain-token-service && python3 -m multiversx_sdk_cli.cli contract build",
    "build:ci:interchain-token-factory": "cd interchain-token-factory && python3 -m multiversx_sdk_cli.cli contract build",
    "build:ci:token-manager": "cd token-manager && python3 -m multiversx_sdk_cli.cli contract build",
    "build:ci:ping-pong": "cd ping-pong-interchain && python3 -m multiversx_sdk_cli.cli contract build",
    "test": "vitest run",
    "test:cargo": "cargo test",
    "test:all": "npm run build && npm run test && npm run test:cargo",
    "typecheck": "tsc --noEmit",
    "interact:devnet": "CHAIN=devnet tsx interact/index.ts",
    "interact:testnet": "CHAIN=testnet tsx interact/index.ts",
    "interact:mainnet": "CHAIN=mainnet tsx interact/index.ts"
  },
  "devDependencies": {
    "@multiversx/sdk-core": "^12.5.0",
    "@multiversx/sdk-wallet": "^4.2.0",
    "@types/keccak": "^3.0.3",
    "@types/node": "^18.16.18",
    "ethers": "^6.9.0",
    "commander": "^11.1.0",
    "tsx": "^3.12.7",
    "typescript": "^5.0.4",
    "vitest": "^0.33.0",
    "xsuite": "0.0.50",
    "keccak": "^3.0.3"
  }
}
