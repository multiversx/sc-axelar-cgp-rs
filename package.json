{
  "private": true,
  "scripts": {
    "build": "npm run build:gateway && npm run build:auth && npm run build:gas-service && npm run build:its && npm run build:remote-address-validator && npm run build:token-manager-mint-burn && npm run build:token-manager-lock-unlock && npm run build:ping-pong",
    "build:cgp": "npm run build:gateway && npm run build:auth && npm run build:gas-service",
    "build:gateway": "cd gateway && mxpy contract build",
    "build:auth": "cd auth && mxpy contract build",
    "build:gas-service": "cd gas-service && mxpy contract build",
    "build:its": "cd interchain-token-service && mxpy contract build",
    "build:remote-address-validator": "cd remote-address-validator && mxpy contract build",
    "build:token-manager-mint-burn": "cd token-manager-mint-burn && mxpy contract build",
    "build:token-manager-lock-unlock": "cd token-manager-lock-unlock && mxpy contract build",
    "build:ping-pong": "cd ping-pong-interchain && mxpy contract build",
    "test": "vitest run",
    "test:all": "npm run build && npm run test",
    "typecheck": "tsc --noEmit",
    "interact:devnet": "CHAIN=devnet tsx interact/index.ts",
    "interact:testnet": "CHAIN=testnet tsx interact/index.ts",
    "interact:mainnet": "CHAIN=mainnet tsx interact/index.ts"
  },
  "devDependencies": {
    "@multiversx/sdk-core": "^12.5.0",
    "@multiversx/sdk-wallet": "^4.2.0",
    "@types/keccak": "^3.0.3",
    "@types/node": "^18.16.18",
    "commander": "11.0.0",
    "keccak": "^3.0.3",
    "tsx": "^3.12.7",
    "typescript": "^5.0.4",
    "vitest": "^0.33.0",
    "xsuite": "0.0.34"
  }
}
